# Rust
/target
**/target
Cargo.lock.bak

# Node
node_modules
dist
.vite
*.tsbuildinfo

# Tauri
apps/desktop/src-tauri/target
apps/desktop/src-tauri/gen

# Local data — a user's library and annotations must never be committed
*.sqlite
*.sqlite-wal
*.sqlite-shm
/data
/working

# OS
.DS_Store
Thumbs.db

# Editors
.idea
.vscode/*
!.vscode/extensions.json

# Local secrets. Never committed — the Zotero key belongs in your environment
# or in the app's own credential store, never in the repository.
.env
.env.local
*.secret

# AI assistant context and scratch files. Useful locally, not part of the
# project — they describe how someone works, not how the software works.
CLAUDE.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.cursorrules
.aider*
.windsurfrules
.github/copilot-instructions.md

# Device build output from the container toolchain.
/target/device
